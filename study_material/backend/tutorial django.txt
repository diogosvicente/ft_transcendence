a cada dependência instalada, use o comando abaixo para listar essa dependência em um aruqivo, para controle
pip freeze > .\requirements.txt

python -m venv venv
pip install django
django-admin startproject setup . (não cria uma subpasta com o mesmo nome)

LANGUAGE_CODE = 'pt-br'
TIME_ZONE = 'America/Sao_Paulo'

python manage.py runserver
pip install python-dotenv

criar .env com SECRET_KEY

no arquivo settings.py
{
	from pathlib import Path, os
	from dotenv import load_dotenv

	load_dotenv()

	SECRET_KEY = str(os.getenv('SECRET_KEY'))
}

https://www.toptal.com/developers/gitignore/

python manage.py help

// para usar PostgreSQL. em settings.py
pip install psycopg2
pip install psycopg2-binary

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'ft_transcendence',
        'USER': 'postgres',
        'PASSWORD': '123456',
        'HOST': 'localhost',
    }
}

baixar o pgadmin nesse site
https://www.enterprisedb.com/downloads/postgres-postgresql-downloads (caso esteja utilizando Windows)


---

sobre o login

pip install djangorestframework-simplejwt


//SOBRE MULTI LINGUAGENS
instalar o gettext
https://github.com/mlocati/gettext-iconv-windows/releases

python ../manage.py makemessages -l pt_BR -l en -l es
python ../manage.py compilemessages